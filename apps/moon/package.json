{
  "name": "moon-family-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prepare-pages-build.mjs",
    "preview": "vite preview",
    "test:pages": "npm run build && node --test tests/pages.test.mjs tests/worker.test.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name moon"
  },
  "dependencies": {
    "@vitejs/plugin-react": "5.0.4",
    "vite": "6.4.2",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {}
}
